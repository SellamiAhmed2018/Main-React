import React from "react";

const Address = (address) => {
	return(
		<React.Fragment>
			<h3 className={"personal_data"}>My Address is : {address} !</h3>
		</React.Fragment>
	);

}

export default Address;